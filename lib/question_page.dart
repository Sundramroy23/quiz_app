import 'package:flutter/material.dart';

class Gobbar extends StatelessWidget {
  const Gobbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("I am Gobbar")),
    );
  }
}
